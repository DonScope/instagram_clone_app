import 'package:flutter/material.dart';

// Primary Colors
const Color primary = Color(0xFF405DE6); // Instagram's primary blue.
const Color primaryLight = Color(0xFF5851DB); // Lighter shade of the primary blue.
const Color primaryDark = Color(0xFF833AB4); // Darker shade transitioning towards purple.

// Accent Colors
const Color accent = Color(0xFFC13584); // Instagram's magenta accent.
const Color accentLight = Color(0xFFE1306C); // Lighter magenta.
const Color accentDark = Color(0xFFA0275D); // Darker magenta.

// Background Colors
const Color background = Color.fromARGB(104, 214, 214, 214); // White background.
const Color scaffoldBackground = Color(0xFFF8F8F8); // Light gray for scaffold background.
const Color cardBackground = Color(0xFFFFFFFF); // White for card backgrounds.

// Text Colors
const Color textPrimary = Color(0xFF262626); // Dark gray for primary text.
const Color textSecondary = Color(0xFF8E8E8E); // Medium gray for secondary text.
const Color textHint = Color(0xFFB2B2B2); // Light gray for hint text.
const Color textDisabled = Color(0xFFDADADA); // Very light gray for disabled text.

// Button Colors
const Color buttonPrimary = Color(0xFF3897F0); // Bright blue for primary buttons.
const Color buttonSecondary = Color(0xFF26A69A); // Teal for secondary buttons.
const Color buttonDisabled = Color(0xFFB2B2B2); // Gray for disabled buttons.

// Border Colors
const Color border = Color(0xFFDBDBDB); // Light gray for borders.
const Color borderFocused = Color(0xFF3897F0); // Bright blue for focused borders.

// Status Colors
const Color error = Color(0xFFE74C3C); // Red for errors.
const Color warning = Color(0xFFF1C40F); // Yellow for warnings.
const Color success = Color(0xFF2ECC71); // Green for success.
const Color info = Color(0xFF3498DB); // Blue for informational messages.

// Icon Colors
const Color iconPrimary = Color(0xFF262626); // Dark gray for primary icons.
const Color iconSecondary = Color(0xFF8E8E8E); // Medium gray for secondary icons.

// Divider Colors
const Color divider = Color(0xFFDBDBDB); // Light gray for dividers.

// Shadow Colors
const Color shadow = Color(0x1A000000); // Subtle shadow effect.
